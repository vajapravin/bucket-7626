"use strict";
/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License;
 * you may not use this file except in compliance with the Elastic License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
const humps_1 = require("humps");
require("isomorphic-fetch");
const lodash_1 = require("lodash");
const kfetch_1 = require("ui/kfetch");
function fetchOptionsWithDebug(fetchOptions) {
    const debugEnabled = sessionStorage.getItem('apm_debug') === 'true' &&
        lodash_1.startsWith(fetchOptions.pathname, '/api/apm');
    if (!debugEnabled) {
        return fetchOptions;
    }
    return {
        ...fetchOptions,
        query: {
            ...fetchOptions.query,
            _debug: true
        }
    };
}
async function callApi(fetchOptions, { camelcase = true, prependBasePath = true } = {}) {
    const combinedFetchOptions = fetchOptionsWithDebug(fetchOptions);
    const res = await kfetch_1.kfetch(combinedFetchOptions, { prependBasePath });
    return camelcase ? humps_1.camelizeKeys(res) : res;
}
exports.callApi = callApi;
